# Checkpoints

Model checkpoints are intentionally not tracked in Git.

Place the required files here:

```text
ckpt/fruit.pt
ckpt/plant.pt
ckpt/mobile_sam.pt
```

You can override these paths with `--weights`, `--detector-weights`, and `--sam-checkpoint`.
